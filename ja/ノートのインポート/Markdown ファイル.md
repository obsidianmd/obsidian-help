---
permalink: import/markdown
cssclasses:
  - soft-embed
---
Obsidianはノートの主要フォーマットとしてMarkdown `.md` ファイルを使用します。そのため、MarkdownファイルをObsidianにインポートするのは簡単です。多くのアプリがMarkdownファイルを編集したり、このフォーマットにエクスポートしたりできます。

Obsidianの保管庫にMarkdownファイルを追加するには、いくつかの方法があります：

1. [[ファイルエクスプローラー]]ウィンドウにファイルやフォルダをドラッグ＆ドロップする
2. システムのファイルブラウザ（例：WindowsエクスプローラーやmacOSのFinder）を使用して、Obsidianの保管庫フォルダに直接ファイルを移動する

## ImporterでMarkdownファイルをインポートする

インポート内のフォルダを選択したり、`.zip` アーカイブをインポートしたり、フォーマットを標準化したり、インラインタグを[[プロパティ|プロパティ]]に移動したり、テンプレートを適用したりしたい場合は、公式の[[インポーター|Importerプラグイン]]を使用してください。

1. **[[設定|設定]] → コミュニティプラグイン**を開き、[Importerをインストール](obsidian://show-plugin?id=obsidian-importer)します。
2. Importerプラグインを有効にします。
3. [[コマンドパレット|コマンドパレット]]またはリボンアイコンを使用して**Importer**を開きます。
4. **File format**で**Markdown (.md, .zip)**を選択します。
5. 個別のMarkdownファイル、フォルダ、またはzipアーカイブを選択します。
6. 検出されたフォルダ、出力オプション、生成されたテンプレート、プレビュー例を確認します。
7. **Import**を選択し、インポートが完了するまで待ちます。

インポートされるノートの名前、プロパティ、コンテンツをカスタマイズするには、[[インポーターテンプレート]]を参照してください。

## テンプレート

データのインポート方法を完全にカスタマイズするには、[[インポーターテンプレート|Importerテンプレート]]を使用してください。

![[インポーターテンプレート#Variables]]
