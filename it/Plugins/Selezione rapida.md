---
permalink: plugins/quick-switcher
description: >-
  Selezione rapida è un plugin principale che permette di cercare e aprire note
  usando solo la tastiera.
aliases:
  - Quick switcher
---
Selezione rapida è un [[Plugin principali|plugin principale]] che permette di cercare e aprire note usando solo la tastiera.

## Aprire la Selezione rapida

Ci sono diversi modi per aprire la Selezione rapida, quando è abilitata:
- Premi `Ctrl+O` (o `Cmd+O` su macOS) per aprire la Selezione rapida;
- Nella barra degli strumenti, fai clic su **Apri selezione rapida** ( ![[lucide-file-search.svg#icon]] );
- Su mobile, quando non stai modificando una nota, tocca l'icona più al centro in basso dell'app.

## Aprire una nota con la Selezione rapida

1. [[#Aprire la Selezione rapida]].
2. Digita per cercare una nota per nome o alias.
3. Naviga fino alla nota usando i tasti freccia.
4. Premi `Invio` per aprire la nota selezionata.

> [!info] 
> La funzionalità di completamento automatico passa a un algoritmo di risultati più semplice quando la cassaforte raggiunge 10.000 elementi per mantenere prestazioni ottimali dell'applicazione. 
^search-autocomplete-large

Se il testo non corrisponde a nessuna nota, puoi premere `Invio` per creare una nota con quel nome. Anche se il testo corrisponde a una o più note simili, puoi comunque creare una nota con il nome esatto premendo `Maiusc+Invio`.

Puoi aprire la nota selezionata in una nuova scheda premendo `Ctrl+Invio` (o `Cmd+Invio` su macOS).

> [!tip] Alternare tra due note
> Se il termine di ricerca è vuoto, la Selezione rapida mostra le note più recenti. Alterna tra due note aprendo la Selezione rapida, premendo la freccia `Giù` e poi premendo `Invio`.


> [!info] File esclusi
> I file che corrispondono ai tuoi modelli di [[Settings#File esclusi|File esclusi]] hanno una priorità inferiore nei risultati della Selezione rapida.
